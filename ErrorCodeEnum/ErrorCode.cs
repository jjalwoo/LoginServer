namespace LoginServer.ErrorCodeEnum
{
    public enum ErrorCode
    {
        WhiteSpace = -1,    // 공백
        Suceess,
        Fail,
        Duplication,  // 중복
        NotDuplication
    }
}
